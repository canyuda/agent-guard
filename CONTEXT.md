# agent-guard

AI 编程 Agent 的工具调用安全守卫:经宿主的 PreToolUse hook 拦截工具调用,用三级判定闸门化破坏性操作。本文件是领域术语表,不是设计文档(设计见 docs/specs/)。

## Language

**宿主**:
挂载守卫的 AI 编程工具(ZCode / Claude Code / Cursor)。守卫以 PreToolUse hook 进程形态寄生其中。
_Avoid_: 客户端、平台

**工具调用**:
Agent 请求执行的一次操作(Bash / Write / Edit / `mcp__.*`),守卫的拦截与判定对象。
_Avoid_: 命令(仅是 Bash 类工具调用的输入)

**判定**:
对一次工具调用的破坏性风险(risk 0-3)与红线违反(violation 概率)的评估结论,含原始概率。
_Avoid_: 评分、审计(审计是判定事实的落盘记录)

**三级判定**:
判定结论的三个等级:allow(放行)/ confirm(需人工确认)/ block(禁止)。阈值全在配置,改阈值即时生效、无需清缓存。
_Avoid_: 风险等级(那是指 risk 0-3 阶梯,不是判定等级)

**决策**:
渲染后实际交给宿主的承载:allow / ask / deny。confirm 级由 `degrade_ask_to_deny` 开关决定用 ask 还是 deny 承载。
_Avoid_: 与「判定」混用——判定是三级结论,决策是它在宿主侧的呈现

**判定管道**:
stdin 原文 → 决策 + 审计条目的纯核心。主路径与兜底共用同一决策值(决策只写一次)。
_Avoid_: hook 管道以外的别称

**决策只写一次**:
向宿主的 stdout 恰好输出一段决策 JSON 的保证。兜底不得产生第二段输出;决策无法投递时以非零退出交还宿主。

**兜底**:
管道内部异常时的最后一道防线:仍尽力输出 confirm 级决策,绝不静默放行。审计如实记录实际输出的决策。
_Avoid_: 降级(degrade 已被配置开关占用)

**fail-closed**:
产品不可破坏的承诺:任何失败路径(API 超时、网络错、未配 key、内部异常、投递失败)都不放行——转人工或拦截。

**快路径**:
不经 API 直接给出判定的名单捷径:MCP 豁免、白名单(明确只读)、黑名单(明确破坏)。
_Avoid_: 白名单(只是快路径的一层)

**判定缓存**:
以「工具 + 归一化输入」为键的本地缓存;存原始判定概率,阈值现算,改阈值不用清缓存。

**审计**:
逐行 JSONL 记录每次判定的事实,`rendered` 字段恒等于宿主实际收到的决策;是调阈值与回溯误判的数据源。

**数据目录**:
`~/.agent-guard/`,运行时配置、判定缓存与审计日志的家。仓库内同名文件只是模板与开发期旧位置。
